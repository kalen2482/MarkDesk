# MarkDesk 产品需求文档 (PRD)

> 版本：v1.0 | 更新日期：2026-07-31 | 状态：已实现

---

## 1. 产品概述

### 1.1 产品定位

MarkDesk 是一款面向 Windows 平台的全功能 Markdown 编辑器，界面设计参照 WPS 智能文档风格。支持源码编辑、分栏同步、可视化 WYSIWYG 三种编辑模式，满足从 Markdown 初学者到高级用户的全链路需求。

### 1.2 目标用户

| 用户类型 | 核心诉求 | 对应功能 |
|---------|---------|---------|
| Markdown 初学者 | 不懂语法也能排版 | 可视化模式 + 工具栏按钮 |
| 技术开发者 | 高效编写文档 | 源码模式 + 代码高亮 + Mermaid |
| 产品/运营人员 | 所见即所得编辑 | 分栏模式 + 实时预览 |
| 知识管理者 | 结构化笔记 | 大纲导航 + TOC + 脚注 |

### 1.3 设计原则

1. **唯一真值**：Markdown 文本始终是唯一数据源，富文本仅为视图层
2. **零门槛**：工具栏覆盖所有常用操作，不需要记忆 Markdown 语法
3. **兼容性**：输出的 Markdown 可被 Obsidian / Typora / VS Code 等主流编辑器正常打开
4. **本地优先**：所有数据存储在本地，不依赖云服务

---

## 2. 功能需求

### 2.1 编辑模式

| 模式 | 说明 | 快捷键 |
|------|------|--------|
| 源码模式 | 纯 Markdown 文本编辑，左侧显示行号 | Ctrl+E |
| 分栏模式 | 左侧源码 + 右侧实时预览，双向同步 | 默认模式 |
| 可视化模式 | 隐藏源码，直接在渲染结果上编辑（类 Notion） | Ctrl+R |

#### 2.1.1 双向同步规则

- 源码编辑 → 预览区实时更新（markdown → HTML）
- 可视化编辑 → 源码区实时更新（HTML → markdown，通过 Turndown）
- 防循环机制：syncSource 来源标记 + 500ms 防抖 + isInternalUpdate ref 三层防护
- 撤销/重做操作正确同步到两个视图

### 2.2 文本格式

| 功能 | 工具栏 | 快捷键 | Markdown 语法 |
|------|--------|--------|--------------|
| 加粗 | B | Ctrl+B | `**文本**` |
| 斜体 | I | Ctrl+I | `*文本*` |
| 删除线 | S | — | `~~文本~~` |
| 下划线 | U | — | `<u>文本</u>` |
| 行内代码 | `< >` | — | `` `代码` `` |
| 标题 H1-H6 | 下拉框 | Ctrl+1~6 | `# 标题` |
| 正文 | 下拉框 | Ctrl+0 | 普通段落 |

### 2.3 字号与颜色

| 功能 | 说明 | 存储格式 |
|------|------|---------|
| 字号下拉框 | 11 种预设（12-32px），可调整编辑器全局字号 | `<span style="font-size: Xpx">` |
| A+ 增大字号 | 选中文本逐级放大 | `<span style="font-size: larger">` |
| A- 减小字号 | 选中文本逐级缩小 | `<span style="font-size: smaller">` |
| 字体颜色 | 16 色预设 + 自定义取色器 + 清除颜色 | `<span style="color: X">` |
| 背景颜色 | 16 色预设 + 自定义取色器 | `<span style="background-color: X">` |

### 2.4 列表与引用

| 功能 | 工具栏 | Markdown 语法 |
|------|--------|--------------|
| 无序列表 | 按钮 | `- 文本` |
| 有序列表 | 按钮 | `1. 文本` |
| 任务列表 | 按钮 | `- [ ] 文本` |
| 增加缩进 | 按钮 | `  文本` |
| 减少缩进 | 按钮 | — |
| 引用块 | 按钮 | `> 文本` |
| 高亮标记 | 按钮 | `==文本==` |

### 2.5 插入元素

| 功能 | 说明 | Markdown 语法 |
|------|------|--------------|
| 超链接 | 弹窗输入 URL | `[文本](url)` |
| 图片 | 弹窗输入 URL / 粘贴图片 | `![描述](url)` |
| 表格 | 插入 3×3 默认表格 | GFM 表格语法 |
| 代码块 | 插入 typescript 代码块 | ` ```lang ` |
| 分隔线 | 水平分隔线 | `---` |
| Mermaid 图表 | 流程图/时序图等 | ` ```mermaid ` |
| Callout 提示框 | 5 种类型（INFO/WARNING/TIP/NOTE/DANGER） | `> [!TYPE]` |
| 脚注 | 引用 + 定义 | `[^1]` + `[^1]: 内容` |
| 目录 | 自动生成 | `[TOC]` |
| 数学公式 | 块级 + 行内 | `$$E=mc^2$$` / `$E=mc^2$` |
| 日期时间 | 当前日期时间 | — |
| Emoji | 8 分类选择器 + 搜索 | — |

### 2.6 文件操作

| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 新建文件 | Ctrl+N | 创建新标签页 |
| 打开文件 | Ctrl+O | 支持 .md/.markdown/.txt/.mdx |
| 保存 | Ctrl+S | 下载为 .md 文件 |
| 另存为 | Ctrl+Shift+S | 指定文件名保存 |
| 导出 HTML | — | 带样式的独立 HTML 文件 |
| 导出 PDF | — | 调用浏览器打印 |
| 拖放打开 | — | 拖放 .md 文件到编辑器 |
| 粘贴图片 | — | Ctrl+V 粘贴剪贴板图片 |

### 2.7 多标签页

- 每个标签页独立维护内容、撤销/重做历史
- 标签页切换时历史保持不丢失
- 关闭标签页时自动切换到相邻标签

### 2.8 撤销/重做

- 每个标签页独立的历史栈（最多 100 步）
- 支持 Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z
- 可视化模式下撤销/重做正确同步到预览

### 2.9 搜索替换

| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 搜索 | Ctrl+F | 支持正则表达式 |
| 替换 | Ctrl+H | 单次替换 |
| 全部替换 | — | 批量替换 |
| 区分大小写 | — | 选项开关 |

### 2.10 侧边栏

#### 大纲导航
- 自动提取 H1-H6 标题生成树形结构
- 点击标题跳转到对应位置（源码/分栏/可视化模式均支持）
- 当前光标所在标题高亮

#### 文件列表
- 显示当前文件信息
- 最近打开的文件记录（最多 10 个）

### 2.11 界面交互

| 功能 | 说明 |
|------|------|
| 深色模式 | 浅色/深色主题切换，Mermaid 图表自适应 |
| 专注模式 | F11 全屏编辑 |
| 工具栏 Tooltip | 悬停显示功能名称 + 快捷键 + Markdown 语法 |
| 右键菜单 | 剪切/复制/粘贴/加粗/斜体/链接/表格/代码块 |
| 快捷键帮助 | Ctrl+/ 查看所有快捷键 |
| 设置面板 | 字体大小/Tab 宽度/自动换行/行号/拼写检查/自动保存/滚动同步/默认模式 |
| 滚动同步 | 分栏模式下编辑器与预览区滚动联动 |
| 可拖拽分隔线 | 调整侧边栏宽度、编辑器/预览比例 |

### 2.12 自动保存

- 定时保存到浏览器 localStorage（默认 30 秒）
- 页面关闭时保存
- 重新打开时恢复上次内容

---

## 3. 非功能需求

### 3.1 性能

- 首屏加载 < 2 秒（本地运行）
- 文档编辑无感知延迟（< 50ms）
- Mermaid/KaTeX 异步渲染不阻塞编辑
- 500ms 防抖避免频繁反向转换

### 3.2 安全性

- DOMPurify 消毒所有 HTML 输出
- 禁止 script/iframe/form 等危险标签
- 禁止 onerror/onload 等事件属性
- Mermaid securityLevel: strict

### 3.3 兼容性

- 输出的 Markdown 兼容 Obsidian / Typora / VS Code
- 颜色/字号/下划线使用 HTML 内联标签（Obsidian 兼容）
- 标准 GFM 语法，不使用私有扩展

### 3.4 可打包

- Vite 构建产出静态文件
- 可通过 Electron / Tauri 打包为 Windows 可执行文件

---

## 4. 技术约束

| 约束 | 选型 | 理由 |
|------|------|------|
| 前端框架 | React 18 + TypeScript 5.5 | 生态成熟，类型安全 |
| 构建工具 | Vite 5.4 | 极速 HMR，零配置 |
| 样式方案 | Tailwind CSS 3.4 | 原子化 CSS，快速开发 |
| Markdown 解析 | marked 12 + marked-highlight + marked-gfm-heading-id | GFM 完整支持 |
| 代码高亮 | highlight.js 11.9 | 200+ 语言支持 |
| 数学公式 | KaTeX 0.18.1 | 快速渲染，无 MathJS 依赖 |
| 图表 | Mermaid 11.16 | 流程图/时序图/甘特图 |
| HTML→MD | Turndown 7.2 + turndown-plugin-gfm | 可靠的反向转换 |
| HTML 消毒 | DOMPurify 3.0.9 | XSS 防护 |

---

## 5. 版本规划

### v1.0（当前已实现）

- 三种编辑模式 + 双向同步
- 完整 Markdown 语法支持（GFM + 扩展语法）
- 工具栏 + 插入面板 + 右键菜单
- 多标签页 + 撤销重做 + 搜索替换
- 大纲导航 + 深色模式 + 自动保存
- 导出 MD / HTML / PDF

### v1.1（规划中）

- Electron 打包为 Windows 可执行文件
- 文件系统直接读写（不再依赖浏览器下载）
- 图片本地存储

### v1.2（规划中）

- 自定义主题
- 快捷键自定义
- 文档模板
- 多窗口支持
